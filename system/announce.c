#include <xinu.h>
#include <stdio.h>

void announce(void)
{
    // struct procent *thisproc;
    // intmask 	mask;    	/* interrupt mask		*/
    // thisproc = &proctab[currpid];
    
    while(1)
    {
        // mask = disable();
        // kprintf("%s", thispric->prname);
        // restore(mask);
    }

}